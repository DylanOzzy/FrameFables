{{#each story as |story| }}
<div id="story">
    <div>
        <h2>
            <a href="/story/{{story.id}}">{{story.name}}</a>
        </h2>
    </div>

    <div>
        <p>
            {{story.description}}
        </p>
    </div>
</div>
{{/each}}